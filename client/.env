# Navify Frontend Portal Configuration
# ====================================

# Frontend Server Port (Used by Vite preview/dev if configured, otherwise default 5173)
PORT=3001

# Backend API URL (For Local Development)
VITE_BACKEND_URL=http://localhost:3000
 PORT: 3000
 NODE_ENV: production